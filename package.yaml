name: minesweeper
dependencies:
  - base
  - array
executables:
  minesweeper:
    source-dirs:
      - src
    main: Main.hs
tests:
  spec:
    source-dirs:
      - test
      - src
    dependencies:
      - hspec
    main: Spec.hs
